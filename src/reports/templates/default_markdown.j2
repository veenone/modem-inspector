# Modem Features Report

---
**Report Metadata**

- **Modem ID:** {{ modem_id or 'Unknown' }}
- **Generated:** {{ generation_time }}
- **Confidence Threshold:** {{ "%.0f"|format(confidence_threshold * 100) }}%
- **Aggregate Confidence:** {{ "%.0f"|format(aggregate_confidence * 100) }}%
- **Total Features:** {{ total_features }}
- **High Confidence Features:** {{ high_confidence_count }}

---

## Summary

This report contains {{ total_features }} features extracted from the modem, with an aggregate confidence score of {{ "%.0f"|format(aggregate_confidence * 100) }}%. Features are categorized into {{ categories|length }} sections, each with confidence indicators showing the reliability of the extracted information.

**Confidence Legend:**
- ✅ **High Confidence** (≥70%): Feature extracted with high reliability
- ⚠️ **Medium Confidence** (30-69%): Feature extracted with moderate reliability
- ❌ **Low Confidence** (<30%): Feature may be inaccurate or incomplete

---

{% if parsing_errors %}
## ⚠️ Parsing Errors

The following errors occurred during feature extraction:

{% for error in parsing_errors %}
- {{ error }}
{% endfor %}

---

{% endif %}
{% if warnings %}
## ⚠️ Warnings

{% for warning in warnings %}
- {{ warning }}
{% endfor %}

---

{% endif %}
{% for category in categories %}
## {{ category.name }}

{% if category.features %}
| Feature | Value | Confidence | Unit |
|---------|-------|------------|------|
{% for feature in category.features %}
| {{ feature.name }} | {{ feature.value }} | {% if feature.confidence >= 0.7 %}✅ {{ "%.0f"|format(feature.confidence * 100) }}%{% elif feature.confidence >= 0.3 %}⚠️ {{ "%.0f"|format(feature.confidence * 100) }}%{% else %}❌ {{ "%.0f"|format(feature.confidence * 100) }}%{% endif %} | {{ feature.unit or '—' }} |
{% endfor %}

{% else %}
*No features available in this category.*

{% endif %}
{% endfor %}

{% if vendor_specific %}
## Vendor-Specific Features

The following vendor-specific features were extracted from the modem:

```json
{{ vendor_specific_json }}
```

{% endif %}

---

## Report Information

- **Report Format:** Markdown
- **Generated By:** Modem Inspector Report Generation System
- **Confidence Threshold:** Features below {{ "%.0f"|format(confidence_threshold * 100) }}% confidence have been filtered out
- **Categories Included:** {{ categories|length }}
- **Total Features Reported:** {{ total_features }}

---

*This report was automatically generated by the Modem Inspector system. For questions or issues, please refer to the system documentation.*
